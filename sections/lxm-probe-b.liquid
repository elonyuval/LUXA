<div>probe b</div>
{% schema %}
{
  "name": "LUXAMOM product — sling",
  "tag": "section",
  "settings": [
    { "type": "text", "id": "badge_text", "label": "Gallery badge", "default": "חדש" },
    { "type": "textarea", "id": "short_desc", "label": "Short description under the price", "default": "מנשא סלינג לכתף אחת שמחזיק את התינוק צמוד אלייך — משחרר לך יד אחת ומוריד את העומס מהגב ומהזרועות." },
    { "type": "text", "id": "age_range", "label": "Age range", "default": "3–36 חודשים" },
    { "type": "text", "id": "max_weight", "label": "Max weight", "default": "עד 20 ק״ג" },
    { "type": "checkbox", "id": "show_stock_note", "label": "Show scarcity strip", "default": true },
    { "type": "text", "id": "stock_count_text", "label": "Scarcity text with a real count", "info": "{count} is replaced by the remaining units of the selected colour. Only shown when inventory tracking is on.", "default": "נשארו רק {count} יחידות" },
    { "type": "range", "id": "low_stock_threshold", "label": "Show the count only below", "min": 3, "max": 50, "step": 1, "unit": "יח", "default": 20 },
    { "type": "text", "id": "stock_note", "label": "Fallback text (inventory not tracked)", "info": "Used when there is no real count to show — keep it free of numbers", "default": "כמות מוגבלת מהמשלוח הנוכחי" },
    { "type": "text", "id": "cta_single", "label": "Button — one unit", "default": "אני רוצה אחד" },
    { "type": "text", "id": "cta_pair", "label": "Button — two units", "default": "אני רוצה שניים" },
    { "type": "text", "id": "pair_tag", "label": "Pair offer tag", "default": "הכי משתלם" },
    { "type": "text", "id": "pair_discount", "label": "Pair discount (₪)", "info": "Must match the automatic \"2 for\" discount in Discounts", "default": "89.99" },
    { "type": "text", "id": "features_heading", "label": "Features heading", "default": "התינוק צמוד, היד שלך פנויה" }
  ],
  "presets": [{ "name": "LUXAMOM product — sling" }]
}
{% endschema %}
